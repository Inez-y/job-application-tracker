export function JobApplicationsPage() {
    return (
        <main className="p-8">
            <h1 className="text-2xl font-bold">Job Applications</h1>
            
            <p className="mt-2 text-slate-600">
                Login worked. Next we will fetch applications from the API.
            </p>
        </main>
    );
}